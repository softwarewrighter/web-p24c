Phase 5: I/O and hardware. Implement: (1) UART output display — show TX bytes in the output console panel with auto-scroll, (2) UART input field — text input with Send button, feed bytes to emulator RX queue, (3) LED D2 indicator — read memory-mapped address 0xFF0200, display green glow when lit, (4) S2 switch toggle — button to toggle switch state, write to memory-mapped address. (5) Wire all I/O to the existing batch execution loop. Verify with demos that produce output (Hello World) and demos that read input (Countdown). Phase 6: status bar + polish — Ready/Running/Exited states in status display, instruction count and binary size stats in hardware panel, Stop button to halt execution. Verify trunk build succeeds and all features work end-to-end.