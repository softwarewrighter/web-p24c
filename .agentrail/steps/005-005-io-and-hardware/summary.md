Wired S2 switch toggle to emulator.set_button_pressed() so hardware input actually reaches the VM. Added output panel auto-scroll via NodeRef and rendered() callback. Added overflow-y:auto to output-text CSS. All Phase 5/6 features were already implemented in Phase 4 (UART I/O, LED D2, S2 toggle UI, status bar, instruction count, binary size, Stop button) — this step completed the two missing pieces.